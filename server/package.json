{
	"name": "server",
	"version": "1.0.0",
	"description": "serve build file",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "cd ../ && git pull origin master && npm run build-no-ts"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"express": "^4.19.2"
	}
}